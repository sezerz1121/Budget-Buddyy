{
  "version": 2,
  "builds": [
    { "src": "./index.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/generate-pdf", "dest": "/index.js" }, // Add specific route for generating PDF
    { "src": "/(.*)", "dest": "/" }
  ]
}
